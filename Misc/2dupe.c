#include <cs50.h>
#include <ctype.h>
#include <math.h>
#include <stdio.h>
#include <string.h>

bool fd(array[])
{
    bool seen[26];
    for (int i = 0; i < 26; i++)
    {
        if (seen[i] == true)
        {
            return true;
        }
        else
        {
            seen[i] = true;
            continue;
        }
    }
}



int main(void)
{
    char sub[] = "ABCDEFGHIJKLMNOPQRSTUVWXYZ"
}